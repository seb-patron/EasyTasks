<!DOCTYPE html>
<html>
<head>
  <title>TestApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
      <%= render 'layouts/navigation' %>

<div id="main-container" class="container">
<div class="pagination-centered">

<div class="row">
        <div class="col-xs-12">
          <% flash.each do |name, msg| -%>
            <%= content_tag :div, msg, class: "alert alert-#{name}" %>
          <% end -%>
        </div>
      </div>
      <div class="row">
        <div class="col-xs-3">
          <h3>Easy Tasks</h3>

          <br>
          <% if current_user%>
          <%= render 'layouts/new_task_form' %>
          <% else %>
          <p>Easy tasks is the quickest, simplest way to create a list of tasks to do. Sign up or login today to get started!</p>
          <% end %>
          <br>


         
        </div>
        <div class="col-xs-9">

          <%= yield %>

        </div>


      </div>
    </div>
   </div>

</body>
</html>
